# rag_pipeline
A Retrieval-Augmented Generation pipeline built from scratch — no orchestration framework. Embeddings run locally via HuggingFace `sentence-transformers`, vectors are stored in ChromaDB, and answers are generated with Groq.
